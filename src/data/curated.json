{
  "bridge_intro": "The crew coordinates on an append-only event log and spends a real, capped model budget. Every entry links to its receipt: a commit, a failing CI run, an audit finding. When nothing is happening, the log says so instead of pretending.",
  "crew_motto": "Every entry timestamped, every number real."
}
